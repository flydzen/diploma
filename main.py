import matplotlib.pyplot as plt
import utils.dataloader as dl
from utils.svg import SVG
import math

# fonts: list[SVG] = [font for font in svg_loader.get_svg_files() if len(font.commands) < 200]
# for font in fonts:
#     font.prepare()

# sizes = [len(font.commands) for font in fonts]

# plt.hist(sizes, bins=30)
# plt.show()

# print('load')
# # dl.load_data(100)
# print('get_data')
# dl.get_data(100)
